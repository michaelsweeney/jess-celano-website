import { useEffect, useState } from "react";
import { Typography } from "@material-ui/core";
const Portfolio = (props) => {
  return (
    <>
      <Typography paragraph>img & download here</Typography>
    </>
  );
};

export default Portfolio;
